Assumes relevant package exists in on premise API package folder.

1. On line 15 and 16 set your unique packageID (Should be the GUID of your package on the server) 
	and also the address of the webapi as the HostAddress ("http://localhost:52948/api/HDCS").
2. The variables and dialogs should be returned in the console variables first then dialogs.